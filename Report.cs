namespace WarehouseWorkshop;
public class Report
{
    //public List<Stock> Items { get; }

    /* Doesn't work */
    // public decimal TotalPrice
    // {
    //     get{
    //         decimal result = 0;
    //         foreach (var stock in Items)
    //         {
    //             result += stock.Item.UnitPrice * stock.Quantity;
    //         }
    //         return result;
    //     }
    // }

    // public Report(List<Stock> items)
    // {
    //     Items = items.Select(stock => new Stock(new Item(stock.Item.Name, stock.Item.UnitPrice), stock.Quantity)).ToList();
    // }
}